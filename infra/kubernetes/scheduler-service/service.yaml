apiVersion: v1
kind: Service
metadata:
  name: scheduler-service
  namespace: cascade-system
  labels:
    app: scheduler-service
spec:
  selector:
    app: scheduler-service
  ports:
    - name: http
      port: 8025
      targetPort: http
