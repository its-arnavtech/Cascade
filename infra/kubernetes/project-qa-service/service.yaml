apiVersion: v1
kind: Service
metadata:
  name: project-qa-service
  namespace: cascade-system
  labels:
    app: project-qa-service
spec:
  selector:
    app: project-qa-service
  ports:
    - name: http
      port: 8040
      targetPort: http
