apiVersion: v1
kind: Service
metadata:
  name: incident-timeline-service
  namespace: cascade-system
  labels:
    app: incident-timeline-service
spec:
  type: ClusterIP
  selector:
    app: incident-timeline-service
  ports:
    - name: http
      port: 8006
      targetPort: http
