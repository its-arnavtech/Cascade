apiVersion: v1
kind: Service
metadata:
  name: topology-service
  namespace: cascade-system
  labels:
    app: topology-service
spec:
  type: ClusterIP
  selector:
    app: topology-service
  ports:
    - name: http
      port: 8004
      targetPort: http
