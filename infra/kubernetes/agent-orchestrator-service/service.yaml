apiVersion: v1
kind: Service
metadata:
  name: agent-orchestrator-service
  namespace: cascade-system
  labels:
    app: agent-orchestrator-service
spec:
  selector:
    app: agent-orchestrator-service
  ports:
    - name: http
      port: 8018
      targetPort: http
