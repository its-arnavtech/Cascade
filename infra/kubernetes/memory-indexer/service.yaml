apiVersion: v1
kind: Service
metadata:
  name: memory-indexer
  namespace: cascade-system
  labels:
    app: memory-indexer
spec:
  type: ClusterIP
  selector:
    app: memory-indexer
  ports:
    - name: http
      port: 8011
      targetPort: http

