apiVersion: v1
kind: Service
metadata:
  name: redpanda
  namespace: cascade-system
  labels:
    app: redpanda
spec:
  type: ClusterIP
  selector:
    app: redpanda
  ports:
    - name: kafka
      port: 9092
      targetPort: kafka
