apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-retrieval-service
  namespace: cascade-system
  labels:
    app: knowledge-retrieval-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: knowledge-retrieval-service
  template:
    metadata:
      labels:
        app: knowledge-retrieval-service
    spec:
      containers:
        - name: knowledge-retrieval-service
          image: cascade-knowledge-retrieval-service:dev
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8016
          env:
            - name: CLICKHOUSE_HOST
              value: clickhouse.cascade-system.svc.cluster.local
            - name: CLICKHOUSE_HTTP_PORT
              value: "8123"
            - name: CLICKHOUSE_DATABASE
              value: cascade
            - name: QDRANT_URL
              value: http://qdrant.cascade-system.svc.cluster.local:6333
            - name: QDRANT_KNOWLEDGE_COLLECTION
              value: cascade_knowledge_base
            - name: EMBEDDING_DIMENSIONS
              value: "128"
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
