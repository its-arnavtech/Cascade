apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-ingestion-service
  namespace: cascade-system
  labels:
    app: knowledge-ingestion-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: knowledge-ingestion-service
  template:
    metadata:
      labels:
        app: knowledge-ingestion-service
    spec:
      containers:
        - name: knowledge-ingestion-service
          image: cascade-knowledge-ingestion-service:dev
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8015
          env:
            - name: CLICKHOUSE_HOST
              value: clickhouse.cascade-system.svc.cluster.local
            - name: CLICKHOUSE_HTTP_PORT
              value: "8123"
            - name: CLICKHOUSE_DATABASE
              value: cascade
            - name: QDRANT_URL
              value: http://qdrant.cascade-system.svc.cluster.local:6333
            - name: QDRANT_KNOWLEDGE_COLLECTION
              value: cascade_knowledge_base
            - name: EMBEDDING_DIMENSIONS
              value: "128"
            - name: KNOWLEDGE_ROOT_PATH
              value: /app/knowledge
            - name: CHUNK_SIZE
              value: "1000"
            - name: CHUNK_OVERLAP
              value: "150"
            - name: KNOWLEDGE_BACKGROUND_ENABLED
              value: "false"
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
