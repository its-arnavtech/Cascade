apiVersion: v1
kind: Service
metadata:
  name: agent-tool-gateway
  namespace: cascade-system
  labels:
    app: agent-tool-gateway
spec:
  selector:
    app: agent-tool-gateway
  ports:
    - name: http
      port: 8017
      targetPort: http
