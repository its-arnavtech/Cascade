apiVersion: v1
kind: Service
metadata:
  name: retrieval-service
  namespace: cascade-system
  labels:
    app: retrieval-service
spec:
  type: ClusterIP
  selector:
    app: retrieval-service
  ports:
    - name: http
      port: 8012
      targetPort: http
