apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-catalogue-once
  namespace: chaos-mesh
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - cascade-targets
    labelSelectors:
      app: catalogue
  duration: "30s"
